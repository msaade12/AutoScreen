# Golden Rules

## NEVER commit without explicit permission
- Do NOT run `git commit` or `git push` without the user explicitly asking
- Make changes to files, but wait for user approval before committing
- Show the user what changed and let them decide when to commit
